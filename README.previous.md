# Air 2 interactive H5 demo

Open `index.html` in a modern browser, or serve this directory locally:

```sh
python3 -m http.server 4173 --directory air2-h5-demo
```

Demo path: Start pumping → Fit Check → enable Auto Switch → simulate milk let-down → choose Milk Boost → Stop pumping → save milk amount → result.
