/* Figma 15:11529 is the only base artwork used here. No CSS-drawn vessel, mirroring, or recolouring. */
function c30Stage(amount) { return amount < 1 ? 'empty' : amount < 2.5 ? 'low' : amount < 5 ? 'medium' : 'high'; }
function c30Drops(stage) { if (stage === 'empty') return ''; const n = stage === 'low' ? 1 : stage === 'medium' ? 3 : 6; return `<span class="c30-drops c30-${stage}" aria-hidden="true">${Array.from({length:n}, (_, i) => `<img src="./assets/figma-control-r15/milk-drop.svg" style="--drop:${i}" alt="">`).join('')}</span>`; }
function c30Vessel(side, amount) { const stage = c30Stage(amount), right = side === 'r', source = `./assets/figma-control-r28/vessel-${right ? 'r' : 'l'}-empty.svg`, level = stage === 'low' ? 22 : stage === 'medium' ? 34 : 46, label = stage === 'empty' ? '0 oz' : `${amount.toFixed(1)} oz`; return `<div class="v4-pump c30-pump ${right?'right':''}"><span class="c30-vessel ${right?'c30-right':'c30-left'} c30-${stage}"><img class="c30-base" src="${source}" alt="Milk container">${stage==='empty'?'':`<span class="c30-liquid-clip"><span class="c30-liquid" style="--liquid-height:${level}px"><svg viewBox="0 0 100 16" preserveAspectRatio="none" aria-hidden="true"><path d="M0 8 Q12.5 4 25 8 T50 8 T75 8 T100 8 V16 H0Z"/></svg><i></i></span></span>`}${c30Drops(stage)}</span><span class="amount">${label}</span></div>`; }
v4Hardware = function (side, amount) { return state.running ? c30Vessel(side, amount) : c15Hardware(side, amount); };
if (state.reviewFrozen && String(state.reviewScreenId || '').startsWith('control-')) root.innerHTML = v4Control();
